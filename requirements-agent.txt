# ═══════════════════════════════════════════════════════
# CulinaryVLM — Agent Layer Requirements (OPTIONAL)
# Install: pip install -r requirements-agent.txt
# Only needed if agent.enabled=true in config.yaml
# ═══════════════════════════════════════════════════════

# ── LangChain Core ──
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0

# ── LangGraph ──
langgraph>=0.2.0
langgraph-checkpoint>=0.2.0

# ── LangChain Provider Integrations ──
langchain-groq>=0.1.0
langchain-google-genai>=1.0.0
