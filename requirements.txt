# ═══════════════════════════════════════════════════════
# CulinaryVLM — Core Requirements (API + Pipeline, no GPU)
# Install: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════

# ── FastAPI Backend ──
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0

# ── Database ──
sqlalchemy>=2.0.23
aiosqlite>=0.19.0

# ── Data Processing ──
openpyxl>=3.1.2
pyyaml>=6.0.1
pandas>=2.1.0
numpy>=1.24.0

# ── NLP / Embeddings (CPU) ──
sentence-transformers>=2.2.2
spacy>=3.7.0

# ── Vector Store ──
faiss-cpu>=1.7.4

# ── API Clients ──
groq>=0.4.0
google-generativeai>=0.3.0
huggingface-hub>=0.19.0
requests>=2.31.0

# ── Video Download ──
yt-dlp>=2023.11.0

# ── Image Processing ──
Pillow>=10.1.0
imagehash>=4.3.1

# ── Environment ──
python-dotenv>=1.0.0

# ── HTTP ──
httpx>=0.25.0

# ── VLM Inference (HuggingFace Spaces) ──
gradio_client>=1.0.0
